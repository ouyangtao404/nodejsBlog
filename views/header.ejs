<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8" />
<title>Blog</title>
<link rel="icon" href="images/favicon.ico" mce_href="favicon.ico" type="image/x-icon">
<link rel="shortcut icon" href="favicon.ico" mce_href="images/favicon.ico" type="image/x-icon">
<link rel="stylesheet" href="/public/stylesheets/global.css">
<link rel="stylesheet" href="/public/stylesheets/style.css">
</head>
<body>


<header class="top-bar">
<p class="tips">
    <% if(user){ %>尊敬的 <span class="red"><%= user.name %></span>，<% } %> 欢迎莅临~
    <% if(locals.user){ %>
    <span>，<a title="登出" href="/logout">退出登录</a></span>
    <% } else { %>
    <span><a title="登录" href="/login">登录</a></span>
    <span><a title="注册" href="/reg">注册</a></span>
    <% } %>

</p>
<h1><%= title %></h1>
</header>

<nav>
<span><a title="主页" href="/">HOME</a></span>
<% if(locals.user){ %>
  <span><a title="发表" href="/post">post</a></span>
<% } %>
</nav>

<article>

<% if (locals.success) { %>
  <div ><%= locals.success %></div>
<% } %>
<% if (locals.error) { %>
  <div><%= locals.error %> </div>
<% } %>